import React from 'react';

// This component is no longer needed since we're removing Arabic language support
// Keeping it as an empty component for now to avoid breaking any imports

const LanguageToggle = () => {
  return null;
};

export default LanguageToggle;
